#
# Generated by the NeXT Interface Builder.
#
# NOTE: Do NOT change this file -- Interface Builder maintains it.
#
# Put all of your customizations in files called Makefile.preamble
# and Makefile.postamble (both optional), and Makefile will include them.
#

MAKEFILEDIR = /usr/lib/nib/
NAME = EasyMail
INTERFACES = AddressBook.nib AddressHelpPanel.nib EasyMail.nib \
             FilePanel.nib HelpPanel.nib InfoPanel.nib MBoxWindow.nib \
             Preferences.nib ReadHelpPanel.nib ReadWindow.nib \
             SendHelpPanel.nib SendWindow.nib
CLASSES = EasyMail.m
MFILES = EasyMail_main.m
LIBS = -lNeXT_s -lsys_s
INSTALLDIR = /LocalApps
APPICON = EasyMail.tiff

ICONSECTIONS =	-segcreate __ICON app EasyMail.tiff

-include Makefile.preamble

include $(MAKEFILEDIR)Makefile.common

-include Makefile.postamble

-include Makefile.dependencies
